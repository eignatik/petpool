/**
 * The repositories implementations to be put here.
 *
 * As we don't want to have a tight coupling and dependency to the only one persistence framework,
 * we use abstract repositories and implement them under the external infrastructure package.
 */
package com.petpool.infrastructure.persistence.hibernate;